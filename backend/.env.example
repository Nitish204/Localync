# Copy this file to .env (or export these directly) before running the
# backend.

# JWT signing secret — always set a real one outside of local dev.
LOCALYNC_SECRET_KEY=change-me-to-a-long-random-string

# No payment gateway is wired in right now (checkout places the order
# directly). See README.md's "Payments" note for how to add one back in.
